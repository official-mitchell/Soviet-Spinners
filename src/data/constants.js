// Storage key and default values for session persistence.
// Created: 2026-08-05 — Data layer constants for Hot Takes Night.

export const STORAGE_KEY = 'soviet-spinners-session';

export const DEFAULT_SLOT_TITLE = 'New Slot';

export const DEFAULT_TOTAL_ROUNDS = 20;

export const REVEAL_MODES = Object.freeze({
  IMMEDIATE: 'immediate',
  GATED: 'gated',
});
